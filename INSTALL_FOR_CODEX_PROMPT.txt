Please fully install and configure this project on this Mac (do the work, not just instructions):
1) Run `./CodexTouchBarInstaller.command` from this folder.
2) Then run `codex-touchbar connect --codex-home ~/.codex` (if command is not in PATH, use `~/Library/Application Support/CodexTouchBar/bin/codex-touchbar connect --codex-home ~/.codex`).
3) Run `codex-touchbar status` and `codex-touchbar btt-commands`, then return final usable status plus the exact BTT script paths.
4) If BetterTouchTool is installed, configure all 4 Touch Bar widgets (DONE / RUN / CHAT / APPROVE) and set the APPROVE click-action script.
5) After completion, reply only with final results (done/run/approve values and the configured script paths).
